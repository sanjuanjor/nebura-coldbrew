<div class="hero-page">
	<header class="topbar">
		<a href="#" class="brand" aria-label="Nebura Coffee">
			<span class="brand-text">NEBURA<br />COFFEE</span>
			<span class="brand-mark" aria-hidden="true"></span>
		</a>

		<nav aria-label="Main navigation">
			<ul>
				<li><a href="#about">About</a></li>
				<li><a href="#technology">Technology</a></li>
				<li><a href="#people">People &amp; culture</a></li>
				<li><a href="#contact">Contact</a></li>
			</ul>
		</nav>
	</header>

	<main class="hero-content">
		<h1>
			Cold brew.<br />
			Reinvented with ultrasound.
		</h1>
		<p>Extracted in minutes, not hours.</p>
		<div class="actions">
			<a class="btn btn-primary" href="#technology">Discover the technology</a>
			<a class="btn btn-secondary" href="#about">Get to know Nebura</a>
		</div>
	</main>
</div>

<style>
	:global(body) {
		font-family: Inter, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
		color: #fff;
	}

	.hero-page {
		position: relative;
		min-height: 100vh;
		padding: 28px 48px;
		background-image:
			radial-gradient(circle at center, rgba(10, 8, 6, 0.22), rgba(8, 6, 4, 0.86) 74%),
			url('/hero-coldbrew.png');
		background-size: cover;
		background-position: center;
		overflow: hidden;
	}

	.topbar {
		display: flex;
		justify-content: space-between;
		align-items: center;
		max-width: 1180px;
		margin: 0 auto;
	}

	.brand {
		display: inline-flex;
		align-items: center;
		gap: 12px;
		padding: 12px 18px;
		background: rgba(255, 255, 255, 0.95);
		border-radius: 6px;
		text-decoration: none;
	}

	.brand-text {
		font-size: 15px;
		font-weight: 600;
		line-height: 1.1;
		letter-spacing: 0.08em;
		color: #2f2f2f;
	}

	.brand-mark {
		width: 24px;
		height: 24px;
		border-radius: 50%;
		background:
			radial-gradient(circle at 62% 38%, rgba(255, 255, 255, 0.75), rgba(0, 0, 0, 0) 50%),
			radial-gradient(circle at 35% 62%, #3c3c3c, #101010 70%);
		box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.18);
	}

	nav ul {
		list-style: none;
		display: flex;
		gap: 36px;
		margin: 0;
		padding: 0;
	}

	nav a {
		text-decoration: none;
		color: rgba(255, 255, 255, 0.88);
		font-size: 24px;
		font-family: 'Times New Roman', Georgia, serif;
	}

	nav a:hover {
		color: #ffffff;
	}

	.hero-content {
		max-width: 1180px;
		margin: 20vh auto 0;
	}

	h1 {
		margin: 0;
		max-width: 900px;
		font-family: 'Times New Roman', Georgia, serif;
		font-size: clamp(40px, 7vw, 96px);
		line-height: 1.05;
		font-weight: 600;
		letter-spacing: -0.01em;
	}

	p {
		margin: 28px 0 40px;
		font-size: clamp(20px, 2vw, 36px);
		color: rgba(255, 255, 255, 0.82);
	}

	.actions {
		display: flex;
		gap: 18px;
		flex-wrap: wrap;
	}

	.btn {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: 14px 28px;
		border-radius: 999px;
		font-size: 15px;
		font-weight: 500;
		text-decoration: none;
		transition: transform 0.2s ease, background-color 0.2s ease;
	}

	.btn:hover {
		transform: translateY(-1px);
	}

	.btn-primary {
		background: #ffffff;
		color: #1a1a1a;
	}

	.btn-secondary {
		background: rgba(0, 0, 0, 0.2);
		color: #fff;
		border: 1px solid rgba(255, 255, 255, 0.65);
	}

	@media (max-width: 980px) {
		.hero-page {
			padding: 22px 20px;
		}

		.topbar {
			align-items: flex-start;
			gap: 16px;
			flex-direction: column;
		}

		nav ul {
			gap: 16px;
			flex-wrap: wrap;
		}

		nav a {
			font-size: 20px;
		}

		.hero-content {
			margin-top: 16vh;
		}

		p {
			margin-top: 20px;
		}
	}
</style>
